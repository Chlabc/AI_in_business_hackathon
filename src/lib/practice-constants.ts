/** Shared hard rule - must appear in every live persona prompt. */
export const NEVER_END_CALL_RULE =
  "CRITICAL: NEVER end the call, hang up, say goodbye to close the session, or use any end_call tool. Keep the roleplay going until the human ends practice.";
